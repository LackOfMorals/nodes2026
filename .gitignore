# Local credentials — never commit
.env

# Build artifacts
sync/bin/

# Local router artifacts (regenerable — see router/README.md)
router/release/
router/config.json
# Rendered from router/graph.yaml.template — contains the Linear API key
router/graph.yaml
# Local test configs — also contain the Linear API key
router/graph-test-collision.yaml
router/graph-test2.yaml
router/plugins/slack/bin/
router/tools/

# Local neo4j subgraph service binary (make neo4j-api / go build)
neo4j-api/bin/

# Node dependencies (regenerable — see neo4jGraphQLSrv/package.json)
neo4jGraphQLSrv/node_modules/
# Generated plain-SDL for the router (make gen-neo4j-schema; source of truth is neo4jGraphQLSrv/schema.graphql)
neo4jGraphQLSrv/neo4j-plain-schema.graphql

# macOS
.DS_Store
