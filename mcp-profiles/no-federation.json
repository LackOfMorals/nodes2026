{
  "_comment": "NODES 2026 talk — Config B (world without federation). Three separate MCP servers: Linear (official hosted MCP, Streamable HTTP + Bearer), Slack (slack-mcp-server 1.3.0 stdio, bot token, --no-cache — see DEMO-ENV.md Phase 5 / PLAN.md Known issue 7), Neo4j (local canary binary, read-only). __PLACEHOLDERS__ are substituted from the repo .env by scripts/switch-mcp-profile.sh — never fill them in by hand and never commit a rendered copy. Needs internet for the Linear server.",
  "mcpServers": {
    "linear": {
      "url": "https://mcp.linear.app/mcp",
      "headers": {
        "Authorization": "Bearer __LINEAR_API_KEY__"
      }
    },
    "slack": {
      "command": "npx",
      "args": ["-y", "slack-mcp-server@1.3.0", "--transport", "stdio", "--no-cache"],
      "env": {
        "SLACK_MCP_XOXB_TOKEN": "__SLACK_BOT_TOKEN__",
        "SLACK_MCP_LOG_LEVEL": "warn"
      }
    },
    "neo4j": {
      "command": "/Users/jg/Projects/neo4j-mcp-canary-1/dist/neo4j-mcp_darwin_arm64_v8.0/neo4j-mcp-canary",
      "env": {
        "NEO4J_URI": "bolt://localhost:7687",
        "NEO4J_USERNAME": "neo4j",
        "NEO4J_PASSWORD": "__NEO4J_PASSWORD__",
        "NEO4J_DATABASE": "neo4j",
        "NEO4J_READ_ONLY": "true",
        "NEO4J_TELEMETRY": "false",
        "NEO4J_LOG_LEVEL": "warn"
      }
    }
  }
}
